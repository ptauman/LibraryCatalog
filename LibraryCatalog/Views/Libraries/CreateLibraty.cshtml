@model LibraryCatalog.Models.Library

@{
    ViewData["Title"] = "CreateLibraty";
}

<h1>CreateLibraty</h1>

<h4>Library</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateLibraty">
            <div class="form-group">
                <label for="libName" class="control-label">שם הספריה</label>
                <input name="libName" id="libName" class="form-control" />

            </div>
            <div class="form-group">
                <label for="genName" class="control-label">שם הספריה</label>
                <input name="genName" id="genName" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
